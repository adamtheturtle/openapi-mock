"""Tests for add_openapi_to_responses.

The overlapping tests (simple, path_param, path_with_dots, query_params, skips_invalid)
are parametrized in test_parametrized.py and run with both respx and responses.
"""
